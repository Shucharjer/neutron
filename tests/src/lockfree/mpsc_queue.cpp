#include <thread>
#include <neutron/lockfree.hpp>
#include "neutron/detail/lockfree/node.hpp"

using namespace neutron;

int main() {
    {
        //
        // mpsc_queue<int> queue;
        // constexpr auto count = 1024;
        // std::jthread jt([&queue] {
        //     for (auto i = 0; i < count; ++i) {
        //         queue.push_back(i);
        //     }
        // });

        //
    }
    return 0;
}
