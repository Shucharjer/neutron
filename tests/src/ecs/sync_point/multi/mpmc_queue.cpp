#include <neutron/ecs.hpp>

using namespace neutron;
using enum stage;
using enum strategy;

void producer(sync_point<multi, mpmc_queue, output<>> sync_point) {
    //
}

void consumer(sync_point<multi, mpmc_queue, input<>> sync_point) {
    //
}

int main() {
    //

    return 0;
}
